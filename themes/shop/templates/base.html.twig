<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>
    .card {
        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
        max-width: 300px;
        text-align: center;
        font-family: arial;
        margin: auto;
    }

    .price {
        color: grey;
        font-size: 22px;
    }

    .card button {
        border: none;
        outline: 0;
        padding: 12px;
        color: white;
        background-color: #000;
        text-align: center;
        cursor: pointer;
        width: 100%;
        font-size: 18px;
    }

    .card button:hover {
        opacity: 0.7;
    }
    </style>
</head>
<body>
    <header>
        <ul>
            <li>{{ cart }}</li>
        </ul>
    </header>

    {% for product in products %}
    <div class="card">
        <img src="{{ product.image }}" alt="..." style="width:100%">
        <h1>{{ product.title }}</h1>
        <p class="price">{{ product.price }}</p>
        <p>{{ product.description }}</p>
        <p><button onclick="window.location='/store/add/{{product.id}}';">Add to Cart</button></p>
    </div>
    {% endfor %}
</body>
</html>