<!DOCTYPE html>

<html lang="en">
    <head>
        <title>GWM Forum</title>

        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
        <link rel="stylesheet" href="/css/7c80a5e0e4.css">
        <link rel="stylesheet" href="/vendor/css/bootstrap.min.css">
        <link rel="stylesheet" href="/bundles/gwm/css/gwm.min.css">

        <script src="/bundles/jquery/js/jquery-3.5.1.min.js"></script>
        <script src="/vendor/js/bootstrap.min.js" defer></script>
    </head>

    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="#">GWM Forum</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="/forum">Browse <span class="sr-only">(current)</span></a>
                    </li>
                </ul>
                <form class="form-inline my-2 my-lg-0">
                    <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
                </form>
            </div>
        </nav>

        <div class="container mt-3">
            <div class="row">
                <div class="col-12">
                    <ul class="breadcrumb dark-t">
                        <li class="active breadcrumb-link"><a href="">Home</a></li>
                    </ul>
                </div>
            </div>

            <div class="row">
                <div class="col-12">
                    {% if Logged() %}
                        <div class="card dark-t text-white mb-3">
                            <div class="card-body">
                                You are logged in. <a class="btn btn-danger" href="/out">Logout</a>
                            </div>
                        </div>
                    {% else %}
                        <div class="card dark-t text-white mb-3">
                            <div class="card-body">
                                <a href="/auth" class="btn btn-secondary">Existing User ? Sign In</a>
                                <button class="btn btn-secondary">Sign Up</button>
                            </div>
                        </div>
                    {% endif %}
                </div>
            </div>

            <div class="row">
                <div class="col-12">
                    <div class="card dark-t text-white mb-3">
                        <div class="card-header">
                            Forums
                        </div>
                    </div>

                    {% if categories is defined and categories|length > 0 %}
                        {% for category in categories %}
                            <div class="card mb-3">
                                <div class="card-header">
                                    {{ category.title }}
                                </div>

                                <div class="card-body">
                                    ...
                                </div>
                            </div>
                        {% endfor %}
                    {% else %}
                        No topics created.
                    {% endif %}
                </div>
            </div>
        </div>
    </body>
</html>