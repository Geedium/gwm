{extends 'basis.latte'}

{block articles}

<div class="container-fluid no-gutters">

{ifset $articles}

    <div n:if="!$articles">
        <b>No articles found.</b>
    </div>

    <div class="card mb-3" n:foreach="$articles as $article">
        <div class="card-header">
            <b>{$article['title']}</b>
        </div>

        <div class="card-body">
            <b>{$article['content']}</b>
        </div>

        <div class="card-footer">
            <b>{$article['created_at']}</b>
        </div>
    </div>
{/ifset}

</div>

{/block}